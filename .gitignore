# Sensitive files, never commit!
.env.local
.env
*.local
refresh_tokens.txt

# Python
__pycache__/
*.pyc
.venv/

# Build artifacts
node_modules/
dist/

# Local test copy (test-only copy of worker.js, to avoid maintaining it twice)
test/
